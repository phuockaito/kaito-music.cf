import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.kaito.music.vercel.app",
    appName: "Kaito Music",
    webDir: "build",
    bundledWebRuntime: false,
};

export default config;
