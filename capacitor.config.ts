import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.kaitophuoc.music",
    appName: "Kaito Music",
    webDir: "build",
    bundledWebRuntime: false,
};

export default config;
